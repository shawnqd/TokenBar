import Foundation
import TokenBarCore

/// Bundled CLI. Phase 0: stub. Phase 1+: `status` / `config` subcommands.
@main
struct TokenBarCLI {
    static func main() {
        // TODO(phase1): parse subcommands (status / config set-api-key / ...).
        print("tokenbar — scaffold (phase 0)")
    }
}
